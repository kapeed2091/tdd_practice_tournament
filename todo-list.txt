Usecases:
   - User can create a knock-out tournament with configurable number of rounds and start datetime
   - User can get list of all tournaments (id, name, number of rounds, start_datetime, tournament_status)
   - User can subscribe to a tournament which is not yet started
   - User can play matches once the tournament is started
       - User will submit his score in the match
   - User will progress to next round if he wins the match
   - User can get his current round number for a tournament
   - User can get his opponent user profile in any round in a tournament
   - User can get the winner user profile of any tournament
   
Raise Appropriate custom exceptions wherever necessary

Assumptions:
   - Tournament will always be full before start time
   - All players will play all their matches
   - In case of tie, Whoever submits the score first is winner
   - User profile has Name, Age, Gender
   - tournament_status - CAN_JOIN, FULL_YET_TO_START, IN_PROGRESS, COMPLETED.
   - list of all tournaments are ordered in descending order wrt start_datetime
   - when user comes to play match, user match status will change to IN_PROGRESS
        status max length as 20
   - keeping default user match score as -1
   - adding users to match, in tournament round 1
        - match making for round 1 is made randomly
   - deciding match winner based on score, whoever makes more score in the
        match will be the winner
   - picking random match from next round and adding match winner to it
   - keeping name, gender and age optional in DB
   - max length for name as 50
   - keeping default age value as -1

ToDo List:
    User can play matches once the tournament is started
       -
    - create round matches
        - invalid tournament id
        - tournament not started
    - adding users to match, in tournament round
        - invalid tournament id
    - get match winner
        - match users not completed the match
    User will progress to next round if he wins the match
        - invalid current round no
        - invalid tournament id
    User can get his current round number for a tournament
        - invalid user
        - invalid tournament id
    User can get his opponent user profile in any round in a tournament
        - invalid user_id
        - invalid round  no


Completed List
    User can create a knock-out tournament with configurable number of rounds and start datetime
        - create knock-out tournament
        - Accept only future date time for start datetime
        - accept only positive integer for no_of_rounds
        - invalid user
    User can get list of all tournaments (id, name, number of rounds, start_datetime, tournament_status)
        - get list of all tournaments
    User can subscribe to a tournament which is not yet started
        - user subscribing to tournament
        - user does not exist
        - user already subscribed
        - tournament not in CAN_JOIN  status
        - invalid tournament
        - when last member joins the tournament change the status to FULL_YET_TO_START
    User can play matches once the tournament is started
       - play match
       - link match to tournament
       - user can not play, when tournament is not started
       - User will submit his score in the match
       - user not in given match
       - user already played the match
    User will progress to next round if he wins the match
        - create round wise matches for knock out tournament
        - add users to tournament round 1 matches
        - get match winner
        - In case of tie, Whoever submits the score first is winner
        - match winner progress to next round
    User can get his current round number for a tournament
        - get user current round no
    User can get his opponent user profile in any round in a tournament
        - get opponent user profile
        - opponent not set

Time Estimation: 10 Hours
Updated Time Estimation: 12 Hours